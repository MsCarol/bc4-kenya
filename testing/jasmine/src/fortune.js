function tellFortune(child,spouse,loccation,job) {
   var future = "You will live in" +loccation ,"get married to" +spouse,"as a"+job ,"with " +child ,"children";
   console.log(future);
} 
tellFortune(2,"Nduva","Karen","Engineers");

function addList(myTable){
	var table = myTable;
	var rowcount = table.rows.length;
	var row = table.insertRow(rowcount);
    var b = "what the hell";
}